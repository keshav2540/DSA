#include <bits/stdc++.h>
using namespace std;
vector<int>BF(vector<int>nums){ /*treval in all array and calculate sum and compare with target if == to target insert both idx vector and return */}
vector<int>twoPointer(vector<int>nums){/* use two pointer one from start and other from end of array according to condition change values of pointer*/}
vector<int>hashing(vector<int>nums){/* unorderd map use and search in this sum-arr[i] return ans in both index*/}
int main() {
    vector<int>nums={2,6,5,8,1};
    int target=7;
    return 0;
}  