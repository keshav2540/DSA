#include <iostream>
#include <vector>
#include<algorithm>
using namespace std;
int main() {
    for(char st='E';st>='A';st--){
        for(char sta=st;sta<='E';sta++){
            cout<<sta;
        }
   cout<<endl; }
    return 0;
}